version = "1.0"

[metadata]
name = "fix-syntax-errors"
description = "Fix Python syntax errors — verifies agent can diagnose and repair code"
epochs = 3
idd_threshold = 0.6

[agent]
timeout_sec = 600
max_turns = 15

[environment]
test_timeout = 60
