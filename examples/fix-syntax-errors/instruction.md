## Goal

Fix all syntax errors in `/workspace/broken.py` so that it runs without errors and produces correct output.

## Requirements

- The file must be valid Python 3.12 (no syntax errors)
- All existing functions must remain (do not delete functionality)
- The fixed file must produce the output "Result: 42" when executed with `python broken.py`

## Constraints

- Do not rename the file
- Do not add new functions or classes
- Do not change the logic — only fix syntax errors
- Do not modify any other files

## Output

Success criteria:
- `python /workspace/broken.py` exits with code 0
- stdout contains "Result: 42"
