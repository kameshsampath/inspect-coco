version = "1.0"

[metadata]
name = "custom-compose"
description = "Demonstrates custom Docker Compose for eval tasks"
epochs = 3
idd_threshold = 0.6

[agent]
timeout_sec = 600
max_turns = 15

[environment]
test_timeout = 60
