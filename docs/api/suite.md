# Suite

Suite configuration loader — reads `suite.yaml` and discovers tasks.

::: inspect_coco.suite
    options:
      show_root_heading: true
      members:
        - load_suite
        - find_suites
        - merge_defaults
        - Suite
        - SuiteDefaults
        - TaskEntry
